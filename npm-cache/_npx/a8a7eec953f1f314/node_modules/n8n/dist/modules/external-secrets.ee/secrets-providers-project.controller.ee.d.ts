import { Logger } from '@n8n/backend-common';
import { Request, Response, NextFunction } from 'express';
import { ExternalSecretsConfig } from './external-secrets.config';
export declare class SecretProvidersProjectController {
    private readonly config;
    private readonly logger;
    constructor(config: ExternalSecretsConfig, logger: Logger);
    checkFeatureFlag(_req: Request, _res: Response, next: NextFunction): void;
    listConnectionsForAProject(): Promise<never[]>;
}
